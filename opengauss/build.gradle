dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation 'org.opengauss:opengauss-jdbc:7.0.0-RC3-og'
}

tasks.named('shadowJar') {
    manifest {
        attributes('Agent-Label': 'openGauss', 'Main-Class': 'com.dbx.agent.opengauss.OpenGaussAgent')
    }
}
